import { z } from 'zod';
export const uuidSchema = z.string().uuid();
export const jsonObjectSchema = z.record(z.string(), z.unknown());
const dateSchema = z.string().datetime();
export const userSchema = z.object({ id: uuidSchema, nome: z.string().trim().min(1).max(120), email: z.string().email(), status: z.enum(['PENDENTE', 'ATIVO', 'BLOQUEADO']), ultimoLogin: dateSchema.nullable().optional(), createdAt: dateSchema, updatedAt: dateSchema });
export const roomSchema = z.object({ id: uuidSchema, nome: z.string().trim().min(1).max(120), codigoConvite: z.string().min(6).max(64), criadorId: uuidSchema, status: z.enum(['ATIVA', 'PAUSADA', 'ENCERRADA']), createdAt: dateSchema, updatedAt: dateSchema });
export const participantSchema = z.object({ id: uuidSchema, salaId: uuidSchema, usuarioId: uuidSchema, papel: z.enum(['MESTRE', 'JOGADOR']), ativo: z.boolean(), preferenciasView: z.object({ zoom: z.number().finite(), panX: z.number().finite(), panY: z.number().finite(), aba: z.string().max(80).optional() }), joinedAt: dateSchema });
export const boardSchema = z.object({ id: uuidSchema, salaId: uuidSchema, nome: z.string().trim().min(1).max(120), cenaAtivaId: uuidSchema.nullable().optional(), createdAt: dateSchema, updatedAt: dateSchema });
export const sceneSchema = z.object({ id: uuidSchema, tabuleiroId: uuidSchema, nome: z.string().trim().min(1).max(120), backgroundUrl: z.string().url().nullable().optional(), gridConfig: z.object({ enabled: z.boolean(), size: z.number().int().positive(), color: z.string().max(32).optional(), opacity: z.number().min(0).max(1).optional() }), visivel: z.boolean(), createdAt: dateSchema, updatedAt: dateSchema });
export const tokenSchema = z.object({ id: uuidSchema, cenaId: uuidSchema, assetOrigemId: uuidSchema.nullable().optional(), characterSheetId: uuidSchema.nullable().optional(), nome: z.string().trim().min(1).max(120), x: z.number().finite(), y: z.number().finite(), escala: z.number().positive().finite(), statusBarMap: z.record(z.string(), z.object({ current: z.string().min(1), maximum: z.string().min(1), color: z.string().max(32).optional() })).nullable().optional(), customData: jsonObjectSchema.optional() });
export const assetSchema = z.object({ id: uuidSchema, usuarioId: uuidSchema.nullable().optional(), salaId: uuidSchema.nullable().optional(), nome: z.string().trim().min(1).max(160), tipo: z.enum(['TEMPLATE_FICHA', 'NPC_PRESET', 'MAPA', 'TOKEN']), url: z.string().url(), pastaId: uuidSchema.nullable().optional(), visibilidade: z.enum(['PRIVADO', 'SALA', 'ATRIBUIDO', 'GLOBAL']), metadata: jsonObjectSchema.optional(), createdAt: dateSchema, updatedAt: dateSchema });
const sheetFieldSchema = z.object({ id: z.string().trim().min(1).max(80), label: z.string().trim().min(1).max(120), type: z.enum(['TEXT', 'NUMBER', 'BOOLEAN', 'RESOURCE', 'FORMULA']), formula: z.string().max(500).nullable().optional(), required: z.boolean().optional(), attributeId: z.string().trim().max(80).nullable().optional() });
export const sheetFieldsSchema = z.object({ cabecalho: z.array(sheetFieldSchema), atributos: z.array(sheetFieldSchema), recursos: z.array(sheetFieldSchema), pericias: z.array(sheetFieldSchema) });
export const sheetTemplateSchema = z.object({ id: uuidSchema, usuarioId: uuidSchema.nullable().optional(), salaId: uuidSchema.nullable().optional(), nome: z.string().trim().min(1).max(160), sistema: z.string().trim().max(80).nullable().optional(), camposSchema: sheetFieldsSchema, createdAt: dateSchema, updatedAt: dateSchema });
export const characterSheetSchema = z.object({ id: uuidSchema, templateId: uuidSchema, usuarioId: uuidSchema, salaId: uuidSchema.nullable().optional(), nome: z.string().trim().min(1).max(160), dadosPreenchidos: jsonObjectSchema, versao: z.number().int().nonnegative(), createdAt: dateSchema, updatedAt: dateSchema });
export const messageLogSchema = z.object({ id: uuidSchema, salaId: uuidSchema, autorId: uuidSchema, tipo: z.enum(['CHAT', 'ROLAGEM', 'SISTEMA']), conteudo: z.string().trim().min(1).max(4000), secreto: z.boolean(), metadata: jsonObjectSchema.optional(), createdAt: dateSchema });
export const apiErrorSchema = z.object({ code: z.string().min(1), message: z.string().min(1), correlationId: z.string().min(1).optional(), details: jsonObjectSchema.optional() });
//# sourceMappingURL=schemas.js.map